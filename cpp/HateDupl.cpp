#include<bits/stdc++.h>

using namespace std;

int N, K;
int cnt[100005];
int a[200005];
int main(void)
{
    cin.tie(nullptr); ios:sync_with_stdio(false);
    cin >> N >> K;
    for(int i = 0; i <= N; i++)
    {
        cin >> a[i];
        int s = 1
    }
}